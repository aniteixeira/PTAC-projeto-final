import { Link } from "react-router-dom"

export default function Card((video)){

    return (
        lista.map((Cadastro) =>
<div key= {Cadastro.id}>
    <Link to = {}
    
     </div>

        <div className ="card">
            <iframe
            width="853"
            height="480"
            ></iframe>
        </div>
        )
    )
}}